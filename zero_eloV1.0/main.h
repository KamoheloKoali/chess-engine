#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void print_board(void);
char getPiece(int row, int column);
void set_piece(char piece, int row, int column);

#endif
